<%= notify %>
<div class="row">
<%= render 'shared/side_bar' %>
  	
  	<div class="col s9">
  		<h2 class="tab">My URLs</h2>
  		<div class="row">
  			<div class="col s12">
  				  		<div class="divider"></div><br />	
  				<table class="highlight responsive-table">
  <thead>
    <tr>
      <th>Long</th>
      <th>Short</th>
      <th>Since</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
	<% if @links.nil? || @links.empty? %>
		<tr>
			<td colspan="3">
				<strong>No link(s) added yet!</strong>	
			</td>
		</tr>
		<% else %>
			<% @links.each do |link| %>
			<tr>
			  <td><%= link.long %></td>
			  <td><%= full_url(link.short) %></td>
			  <td><%= time_ago_in_words(link.created_at) %></td>
			  <td><%= link_to 'View', show_link_path(link) %></td>
			  <td><%= link_to 'Edit', update_link_path(link) %></td>
			  <td><%= link_to 'Delete', link, method: :delete, data: { confirm: 'Are you sure?' } %></td>
			</tr>
		<% end %>
	<% end %>
  </tbody>
</table>


  				  			</div>
  		</div>
  	</div><!-- ende of talbe div -->



</div>
